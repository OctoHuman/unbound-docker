name: Docker Build and Push

permissions: {}

on:
  schedule:
    # At 5:30am on the 2nd, 15th, and 29th of every month
    - cron: '30 5 2,15,29 * *'

  # Allows manual workflow runs through github.com
  workflow_dispatch:


jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v3

      # Required to cross-compile
      - name: Set up QEMU
        run: |
          sudo apt-get update
          sudo apt-get install qemu-user-static binfmt-support -y

      - name: List available build platforms
        run: docker buildx ls

      - name: Build ARMv8 image
        run: docker build . --tag unbound-armv8:latest --no-cache --platform=linux/arm64/v8

      - name: Build AMD64 image
        run: docker build . --tag unbound-amd64:latest --no-cache --platform=linux/amd64

      - name: Log in to registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u OctoHuman --password-stdin

      - name: Create manifest
        run: docker create manifest ghcr.io/octohuman/unbound:latest unbound-amd64:latest unbound-armv8:latest

      - name: List manifest
        run: docker manifest inspect --verbose ghcr.io/octohuman/unbound:latest

      - name: Push image
        run: |
          docker push ghcr.io/octohuman/unbound:latest

      - name: Log out of registry
        run: docker logout ghcr.io
